ChessGUI is a Python desktop application built with Tkinter and structured using OOP principles to keep GUI elements modular and maintainable. The project uses python-chess as the chess engine and move-validation library.

